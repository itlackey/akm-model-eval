---
captureMode: hot
beliefState: contradicted
inferenceProcessed: true
updated: 2030-05-23
contradictedBy:
  - memory:example-4ba8a72f
description: Tracks the live article curation pipeline location, configuration
  structure, and recent validation metrics.
when_to_use: When needing to locate, understand, or extend the automated article
  curation workflow.
---

# Article Curation Pipeline Status

## Overview
This document tracks the operational status of the article curation pipeline, including its live location, configuration structure, and recent validation results.

## Location & Components
The pipeline is currently active at `$BUNDLE_ROOT/skills/article-curator/` (skill+script) and orchestrated via:
- `$BUNDLE_ROOT/workflows/curate-to-wiki.md` (workflow definition)
- `$BUNDLE_ROOT/tasks/curate-agent-learning.yml` (daily 12:15 task)

Configuration files reside at `$BUNDLE_ROOT/config/curation/<name>.json`, supporting an optional `'wiki'` field.

## Recent Validation (2030-05-23)
In a 60-second window, the pipeline successfully:
- Fetched 50 articles from 5 sources: HN, Semantic Research, OpenAlex, ArXiv AI, MIT Tech Review
- Selected 9 articles via LLM rubric
- Skipped 39 due to paywalls and 2 for other reasons

## Adding New Topics
To introduce new topics:
1. Drop a new `<topic>.json` file into `config/curation/`
2. Update the task definition in `akm tasks` by adding `<id> --schedule '...' --prompt '...'` or copying an existing task.

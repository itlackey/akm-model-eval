---
description: Comprehensive documentation of the PrivilegeGate admin module
  including error mapping flows, modal state flags, naming conventions, and
  registry-to-addons renames.
sources:
  - session:example-2bfd7160
  - session:example-f8adedbd
  - session:example-3eb8106f
  - session:example-d6abcd36
  - session:example-965d5fdb
updated: 2030-06-14
inferenceProcessed: true
---

# System Modules, User Verification & Modal State Assets

## Privilege Gate (Privileged Console)
The `/admin/features` page is wrapped in an `PrivilegeGate`, requiring a valid escalation token before rendering the module management UI.
1. **Environment Check**: The guard checks if the environment is SaaS (`env.DEPLOYMENT_TENANT_ID === CLOUD_TENANT_ID`).
2. **SaaS Behavior**: If SaaS and no valid escalation token exists, it shows an `PrivilegeModal`.
3. **Test Harness**: In `data-platform-tests`, `authenticate(page, { role: 'superadmin' })` pre-seeds tokens, allowing superadmin roles to bypass the guard automatically.

## Page Structure
- **Path**: `/admin/features`
- **Title**: `Module Management`
- **Heading**: `System Modules` inside a `Panel` component.

## UI Elements
- **Buttons**:
  - `Enable All` (secondary small, disabled while saving)
  - `Disable All` (secondary small, disabled while saving)
- **Module Cards Grid**:
  - One card per module (currently: Connectors/Warehouse/Analytics).
  - Each card contains:
    - Icon
    - `h3` with the mapped display name (`ConnectorHub` → `Integrations`, etc.)
    - Description paragraph
    - Checkbox (`role='switch'`) with `id=module.id` to toggle state.
    - Label showing `Enabled` or `Disabled` text based on `module.enabled`.
  - Cards have a `.disabled` CSS class (opacity 0.6) when the module is disabled.

## API Contracts & Data Flow
- **GET**: `/api/data-platform-modules` — Returns the list of modules.
- **PUT**: `/api/data-platform-modules/:id` — Toggles the `enabled` state for a specific module.
- **Flow**:
  1. `FeatureViewModel.loadModules()` calls `FeatureApiClient.getModules()`.
  2. The API client checks cache (`data-platform-modules`) first; if missing, fetches from `/data-platform-modules` and caches for 5 minutes.
  3. Module names are remapped via `mapFeatureNames()` before rendering (e.g., `ConnectorHub` → `Integrations`).
  4. Toggling a module updates local state and calls the API; cache is cleared on update to force refresh on next load.

## User Verification Flow (AUTH-02)
Gap AUTH-02 covers `IdentityCheckApiClient.js` (lines 38–159) and `ConsoleViewModel.svelte.js`. The verification surface has four endpoints and a multi-step login flow with identity linking.

### Client-layer Error Code Mapping (Invariant)
`IdentityCheckApiClient.verifyUser()` wraps raw HTTP status codes into structured objects **before** returning to callers:
- `404` → `{ verified: false, code: 'ACCOUNT_NOT_ELIGIBLE' }`
- `403` → `{ verified: false, code: 'ACCESS_DENIED' }`
- other error → `{ verified: false, code: 'VERIFICATION_FAILED' }`

Other mappings include:
- `linkIdentity()` maps errors to `{ linked: false, code: response.data?.code || 'LINK_FAILED' }`.
- `checkAccess()` maps errors to `{ hasAccess: false }`.
- `verifyUserByEmail()` maps errors to `{ exists: false }`.

These mappings are **client-only** — the backend returns raw status codes; the client normalizes them for UI consumption.

### SaaS-mode Gate (Non-obvious Constraint)
The entire verification flow in `ConsoleViewModel.login()` is dead code on self-install stacks (`DEPLOYMENT_TENANT_ID` mismatch). The `isAccessDenied` UI overlay requires a `@requires-cloud-flow` gate because it never fires on self-install stacks.

### Three-step Link-Retry Flow
When `verifyUser()` returns `ACCOUNT_NOT_ELIGIBLE`, the client triggers `linkIdentity()` followed by a retry of `verifyUser()`. This is an idempotent operation (already-linked identity returns success or a 409 conflict). Accept `[200, 201, 204, 409]`.

### Auth Gate Invariant
All four `/verify/*` endpoints return `403 application/json` (not HTML) without a JWT. This is a critical failure mode for unauthenticated requests.

## Modal State Flags in ReportWorkspace Components

### AssistantPanel.svelte (4,117 lines)
The following state flags control slide-out panels and modals:
- `showDialogb4ca28` — save report to data-store dialog
- `showDialog5682d9` — dashboard mapping configuration
- `showDialogd6c82d` — export format selection (CSV/PDF/Excel)
- `showDialog4cf245` — cleanup confirmation dialog
- `showDialog9823a6` — quick questions panel
- `showDialog68a45e` — downloads panel visibility

### ReportOverview.svelte (4,940 lines)
The following state flags control tile-level modals:
- `showDialoge7c25f` — AI chat for chart analysis
- `showDialog167141` — chart summary dialog
- `showDialogec608e` — link charts configuration
- `showDialog56187f` — naming/editing dialog
- `showDialoge4e00d` — recipe editor modal
- `showDialog7ad912` — tile metadata editing

### ReportAssistant.svelte (7,452 lines)
The following state flags control builder/selector modals:
- `showDialogfc610d` — document upload panel
- `showDialog32ee3e` — dashboard builder canvas
- `showDialog26c2a6` — chart type selection
- `showDialog96b05c` — data refinement options
- `showDialog18a541` — axis configuration
- `showDialog5dcbdc` — template upload dialog
- `showDialog887332` — add visual to canvas

## Coverage Gap
Existing specs cover basic rendering and some flows, but do not explicitly test:
1. Modal open/close cycles for each flag
2. State transitions between different modal states
3. Edge cases where modals fail to render or close
4. RBAC gating of specific modal affordances

This represents a significant coverage gap in the largest components of the codebase.

## Gateway Naming Convention Cleanup (Deprecated References)
**Observation**: Gateway source files contain outdated references to `connector-sdk` as an external library, when it actually refers to the AgentPortal portal runtime with implementations in local modules like `content-filter.ts` and `route-bounds.ts`. The codebase uses `connector-sdk` as a conceptual umbrella term for channel-facing logic, but the naming is misleading; future comments should reference the concrete module names.

During cleanup of stale comments, several files referenced `connector-sdk` as if it were a library or framework, but the actual implementations live in local modules like `content-filter.ts`, `route-bounds.ts`, and `isTurnEnd` from portal runtime. The codebase uses `connector-sdk` as a conceptual umbrella term for channel-facing logic, but the naming is misleading; future comments should reference the concrete module names.

**Affected files include:**
- `containers/gateway/src/forward.ts`: "Uses the shared assistant HTTP client from connector-sdk" → corrected to `(runtime-client.ts)`
- `containers/gateway/src/proxy.ts`: "Endpoint allowlist ... — §3.3, connector-sdk matchAllowlist" → corrected to `route-bounds.ts`
- `containers/gateway/src/moderation.ts`: "Heuristic pre-screen (connector-sdk/content-screen)" → corrected to `(content-filter.ts)`
- `containers/gateway/src/event-router.ts` and `containers/gateway/src/route-bounds.ts`: Referenced `connector-sdk isTurnEnd` → corrected to local definition

**Observation**: The gateway does not import or depend on a `connector-sdk` package; it implements its own client (`runtime-client.ts`) and uses the portal's protocol definitions. These references should be updated to clarify that `connector-sdk` refers to the portal runtime (e.g., "isTurnEnd from the portal runtime", "content-filter.ts: pure, in-process").

### Affected Files and Locations (Detailed):
1. **forward.ts** (Line 6):
   - Original: "Uses the shared assistant HTTP client from connector-sdk."
   - Reality: Uses `./runtime-client.ts` (local module)

2. **proxy.ts** (Lines 10, 98, 273):
   - Line 10: References "§3.3, connector-sdk matchAllowlist" → should be `route-bounds.ts`
   - Line 98: "channel HMAC headers must not leak" → actually refers to inbound auth headers
   - Line 273: "carries fresh per-call signing" → actually refers to Basic auth verification

3. **moderation.ts** (Lines 7, 17):
   - Line 7: "(connector-sdk/content-screen)" → should be `content-filter.ts`
   - Line 17: "structural + HMAC guarantees" → should be "auth guarantees"

4. **event-router.ts** (Line 136):
   - References "connector-sdk isTurnEnd / TURN_IDLE_STATUSES" → actually uses portal runtime's `isTurnEnd`

5. **route-bounds.ts** (Line 186):
   - References "connector-sdk isTurnEnd" → same as above

6. **scripts/oc-e2e.ts** (Lines 3, 6):
   - References "ingress_net network" and "Proves signing" → actually tests Basic auth

### Pattern
The gateway codebase appears to have been designed with a `connector-sdk` abstraction layer that was either:
- Never implemented locally
- Replaced by local modules without updating documentation
- Refactored but comments not updated

### Impact
These stale references create:
- Misleading architectural understanding for new developers
- Potential confusion during code review ("why does this call connector-sdk?")
- False assumptions about external dependencies

## Gateway Channel Auth Guarantees
The gateway's `/oc/*` proxy path relies on structural validation and HMAC guarantees upstream, not just content moderation. In `containers/gateway/src/moderation.ts`, the comment states: "allowed (the structural + HMAC guarantees still apply upstream)." This indicates that even when content moderation is disabled (`GATEWAY_CONTENT_VALIDATION`), the platform retains security guarantees via:
1. Structural validation (payload shape, required fields)
2. HMAC signing for channel authenticity
3. Session ownership and allowlist matching

This is a critical design pattern: **defense in depth**. The gateway doesn't rely solely on content moderation; it layers multiple security mechanisms. Removing or weakening one layer (e.g., disabling moderation) doesn't expose the system to unauthenticated requests because upstream guarantees still hold. This was clarified in a session transcript at 2030-06-13T17:10:03.414Z where the agent updated the comment from "HMAC" to "auth" to reflect this layered security model.

## Gateway Security Architecture Update
The gateway container (`containers/gateway/`) implements a multi-layer security model:

1. **Structural validation** (payload schema)
2. **Basic auth + user identity binding** (per-call authentication)
3. **Endpoint allowlist, default-deny** (route-bounds.ts matchAllowlist)
4. **Session-ownership authz** (permission relay records)
5. **Content moderation** (content-filter.ts heuristic pre-screen)

### Stale Terminology
Multiple files still reference legacy concepts:
- `connector-sdk` (now internal runtime-client.ts)
- `HMAC headers` / `signing` (replaced by Basic auth gates)
- `ingress_net network` (throwaway container has direct access, no host port)

### Evidence
Agent's comment corrections at 2030-06-13T17:09:43.096Z–17:10:17.570Z updating:
- `forward.ts`: "connector-sdk" → "runtime-client.ts"
- `proxy.ts`: "HMAC headers" → "inbound auth headers", "signing" → "Basic auth"
- `moderation.ts`: "connector-sdk/content-screen" → "content-filter.ts"
- `event-router.ts`/`route-bounds.ts`: "connector-sdk isTurnEnd" → "isTurnEnd from portal runtime"
- `scripts/oc-e2e.ts`: "ingress_net network" → "direct access", "signing" → "Basic auth"

### Confidence
High confidence this terminology drift exists across multiple files and should be cleaned up to avoid confusion for future maintainers.

## Output Volume Analysis
Over the last 9 days, `promoted` events are dominated by:
- **consolidate**: ~160–1,260/day (deduping memories)
- **extract**: ~7–1,300/day (new insights from sessions)

Reflect and distill contribute <5% of output in steady state. The "huge" output of June 5–12 was a transient spike (journal bug + backlog drain), not a sustainable baseline. The pipeline is designed to be **reactive** (signal-gated) with a broken proactive lane. Sustained improvement depends entirely on new session input.

## Registry Rename to Addons
As part of the WS-D PR-4 cleanup, `packages/lib/src/control-plane/registry.ts` was renamed to `addons.ts` (via git mv). The rename included:

- **File moves**: `registry.ts` → `addons.ts`, `registry.test.ts` → `addons.test.ts`.
- **Import updates**: 7 sites updated across lib barrel, test files, config-persistence.ts, setup.ts, lifecycle.ts, and two test harnesses.
- **Verification**: `grep -rn "control-plane/registry"` returned nothing; `bun test addons.test.ts` passed all 21 tests.

The rename was bundled with Task A (deletion of CHANNEL_*_SECRET rows) to avoid conflicts in registry.ts. The module now exports addon management functions under the new name.

## References
- Source: `data-platform-src/lib/system/IdentityCheckApiClient.js` lines 38–159
- Source: `data-platform-src/lib/system/ConsoleViewModel.svelte.js` (login() lines 446–672, isAccessDenied state lines 55–59)
- Spec: `data-platform-tests:tests/system/system-user-verification.parity.spec.ts` (26 tests, 7 sections)
- Backend routes: `example-data-api/conf/routes` lines 880–887
- Gateway Cleanup Sources: `containers/gateway/src/forward.ts`, `containers/gateway/src/proxy.ts`, `containers/gateway/src/moderation.ts`, `containers/gateway/src/event-router.ts`, `containers/gateway/src/route-bounds.ts`

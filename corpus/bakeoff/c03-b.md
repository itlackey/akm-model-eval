---
description: The `akm` consolidate pass uses per-process LLM configuration.
  `profiles.improve.default.processes.consolidate`, mirroring patterns used in
  other passes like `extract`. This ensures consistent model selection across
  operations, reducing silent token-budget mismatches.
sources:
  - session:example-2595d92b
updated: 2030-05-27
inferenceProcessed: true
---

# LLM Profile Resolution for Consolidation Pass

The `akm` consolidate pass resolves the specific LLM configuration defined in `profiles.improve.default.processes.consolidate`. This ensures consistent model selection across operations, reducing silent token-budget mismatches that occur when the system falls back to a generic default.

Previously, `akmConsolidate` silently defaulted to `getDefaultLlmConfig(config)`, ignoring any specific model configured for the `processes.consolidate` entry in the user's profiles. This led to silent failures or performance issues when a lighter-weight LLM (e.g., Ministral-3b) was intended for consolidation, as the default configuration often used a long-context model with restrictive runtime limits.
